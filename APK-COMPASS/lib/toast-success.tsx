/**
 * @deprecated Użyj import { toast } from '@/lib/toast' zamiast tego.
 * Ten plik istnieje dla backward compatibility — nowe pliki powinny
 * importować z '@/lib/toast', gdzie toast.success() jest automatycznie animowany.
 */
export { toastSuccess } from '@/lib/toast'
